
<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table class="table table-success table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Unit Cost</th>
      <th>Quantity</th>
      <th>total</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

      <tr>
        <td><%= @order_item.product.title %></td>
        <td><%= @order_item.product.description %></td>
        <td><%= @order_item.product.cost %></td>
        <td><%= @order_item.quantity %></td>
        <td><%= @order.total %></td>

      </tr>

  </tbody>
</table>

<br>
